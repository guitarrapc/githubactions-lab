name: multiline run
on:
  workflow_dispatch:
  push:
    branches: ["main"]

jobs:
  push:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    timeout-minutes: 5
    steps:
      - run: |
          echo "foo"
          echo "bar"
