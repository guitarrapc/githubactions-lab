name: tag push only
on:
  push:
    tags:
      - "**" # only tag

jobs:
  job:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    timeout-minutes: 3
    steps:
      - run: echo not run on branch push
