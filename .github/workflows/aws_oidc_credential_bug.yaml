name: aws oidc credential (bug)

on:
  workflow_dispatch:
  push:
    branches: ["main"]

# allow use id-token
permissions:
  id-token: write
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Cache AWS Credentials (1)
        uses: ./.github/actions/aws_oidc_auth_single
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
      - name: Cache AWS Credentials (2)
        uses: ./.github/actions/aws_oidc_auth_single
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME_2 }}
